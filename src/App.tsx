import { Registration } from "./components/Registration";

export const App = () => {
  return (
    <div className="container">
     <Registration onSubmitForm={() => {}}/>
    </div>
  );
};

export default App;
